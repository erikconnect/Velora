# Core Opera of Scenes — Design

**Date:** 2026-08-27  
**Page:** `apps/showcase/pages/core/core.html`  
**Supersedes:** sparse Release desk pass (same date) — desk kept as failed experiment; Core is now cinema shell + pin/stage/acts.  
**Extended by:** `2026-08-27-core-continuous-track-design.md` (single film track intro→channels).

## Goal

Teach Core through **dense cinematic scenes** (Home/Scenes intensity): shared clocks, `vl-card`, overlays, scroll channels, large type—not a minimal form desk.

## Acts

| ID | Form | Proof |
|---|---|---|
| `#core-opera` | One `vl-pin="10"` scrub (`--vl-beats: 12`) | Hero → architecture → drivers → channels on one sticky stage |
| `#core-intro`…`#core-channels` | Absolute marks on the track | Bussola jumps + `--tl-1…4` |
| `#core-policy` | Cinema + coda (outside pin) | Voice choices + support matrix + Elements CTA |

## Constraints

Zero-JS motion; reuse `scene-tl-*` / cinema CSS; no new `vl-*`; no `vl-in-view` inside pin+scrub stages.
