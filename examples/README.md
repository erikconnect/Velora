# Velora Examples

Production-ready examples using the Velora motion system.

| Example | Path | Shows |
| --- | --- | --- |
| Tailwind host | [`tailwind-host/`](./tailwind-host/) | `@velora/css/motion-core` + Tailwind utilities — no Velora skin |

Showcase (`apps/showcase`) is the cinematic reference UI (Home, Timeline, Skins, Catalog, Hosts, Archive). Older pages: `archive/showcase-2026-08/`.
